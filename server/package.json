{
  "name": "@sol/server",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts"
  },
  "dependencies": {
    "@sol/shared": "workspace:*",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@sol/client": "workspace:*",
    "@types/node": "<=25",
    "typescript": "^5.9.3"
  }
}
